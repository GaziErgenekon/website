# Güncelleme Günlüğü — 16.05.2026

Ergenekon R&D Team web sitesi: hata düzeltmeleri, içerik zenginleştirme ve
geliştirme fazlarının kaydı. Her değişiklikte bu dosya güncellenir.

Teknoloji: React 19 + Vite 7 + Tailwind CSS v4 + React Router v7 (statik /
GitHub Pages). Değişmez kural: mevcut tasarım, renk ve layout korunur;
refactor'larda görsel çıktı birebir aynı kalır.

---

## Faz 0 — Hata Düzeltmeleri, Temizlik ve 5G İçeriği (tamamlandı)

### Düzeltilen hatalar
- **App.jsx**: lazy route'lar `<Suspense>` ile sarılmamıştı (boş ekran/hata
  riski) → `<Suspense fallback={<Loading/>}>` eklendi. Ayrıca
  `/5g-konumlandirma` route'u eklendi.
- **ScrollTop.jsx**: `return;` (undefined → React uyarısı) `return null`
  yapıldı; URL hash'i için lazy sayfaları bekleyen retry'lı kaydırma eklendi
  (footer çapraz-sayfa bağlantıları artık çalışıyor).
- **Footer.jsx**: kırık `#hash` Link'leri `/#hash` yapıldı; eksik `key`
  eklendi; "Bize Katıl" linki + hover eklendi.
- **Stats.jsx**: map'te eksik `key` eklendi.
- **Button.jsx**: yer değiştirmiş `size`/`variant` PropTypes düzeltildi.
- **Map.jsx**: geçersiz `allowTransparency` prop'u kaldırıldı (React 19
  konsol hatası).
- **Contact.jsx**: ölü `("");` ifadesi kaldırıldı; geçersiz
  `hover:primary/40` → `hover:border-primary/40`; LinkedIn linkine
  `target/rel` eklendi.
- **Teams.jsx**: geçersiz `hover:primary/50` → `hover:border-primary/50`;
  LinkedIn linklerine `target/rel/aria-label` eklendi.

### Temizlik
- Kullanılmayan ölü dosya **src/data/content.js** silindi.

### İçerik zenginleştirme
- **Hero.jsx**: ikincil CTA ("İletişim") `outline` varyantına alındı
  (CTA hiyerarşisi).
- **Achievements.jsx**: her karta "ÖDÜL & BAŞARI" / "FAALİYET" rozeti.

### Yeni içerik — 5G Konumlandırma Yarışması
- **src/pages/FiveG.jsx** (yeni): IDEF sayfasıyla birebir aynı tasarım
  yapısında faaliyet sayfası (başlık, hero, "Yarışma Nedir?", KIZILYEL
  vurgusu, istatistikler, basın, galeri).
- **src/constants/index.jsx**: `fiveGContent` verisi eklendi (içerik
  Gazi Üniv. ve Turkcell resmî kaynaklarından doğrulandı).
- **Header.jsx**: "Faaliyetler" menüsüne (masaüstü dropdown + mobil alt
  menü) 5G girişi eklendi.

### Metin iyileştirmesi (kullanıcı geri bildirimi)
- **FiveG.jsx**: "Haber Kaynakları" → **"Basında Biz"**; alt metin
  "Başarımızın ulusal basında ve resmî kaynaklardaki yankısı".
- **constants**: buton etiketleri "Haber Kaynağı"/"Detaylı Bilgi" →
  **"Haberi Oku"/"Bülteni Oku"**.

### Yardımcı
- **.claude/launch.json** (yeni): dev/önizleme sunucusu yapılandırması.

### Doğrulama
- `npm run build` başarılı; canlı önizlemede konsol temiz (key/PropType/
  render uyarısı yok); mobil + masaüstü, footer navigasyonu, 5G sayfası
  ve mobil menü test edildi.

---

## Faz A — SEO, Sayfa Meta, Analytics (tamamlandı)

### A.1 — SEO
- **index.html**: Open Graph + Twitter Card meta'ları, `canonical`,
  `theme-color`, `author` eklendi. OG görseli geçici olarak
  `/logo-full.png` (1200×630 özel görselle değiştirilebilir).
- **public/robots.txt** (yeni): tüm botlara izin + sitemap referansı.
- **public/sitemap.xml** (yeni): 5 route (/, /idef, /5g-konumlandirma,
  /ekibimiz, /bize-katil).

### A.2 — Sayfa bazında meta
- **src/hooks/useDocumentMeta.js** (yeni): bağımlılıksız; `document.title`
  + description/OG/Twitter meta'larını route'a göre günceller, ayrılışta
  varsayılana döner.
- Tüm sayfalara eklendi: Home (varsayılan), Idef ("IDEF'25 Yolculuğumuz"),
  FiveG ("5G Konumlandırma Yarışması"), Ekibimiz, BizeKatil, Error (404).

### A.4 — Cloudflare Web Analytics
- **src/components/common/Analytics.jsx** (yeni): yalnızca beacon;
  `VITE_CF_BEACON_TOKEN` yoksa script hiç eklenmez (güvenli no-op).
  Panel Cloudflare hesabında, siteye hiçbir istatistik yansımaz.
- **MainLayout.jsx**: `<Analytics />` mount edildi.
- **.github/workflows/deploy.yml**: `VITE_CF_BEACON_TOKEN` secret eşlemesi.
- **.env.example** (yeni): tüm VITE_ değişkenleri belgelendi.
- ⚠️ Aktivasyon için: Cloudflare Web Analytics token'ı + GitHub'da
  `CF_BEACON_TOKEN` secret'ı (kullanıcı aksiyonu).

### Doğrulama
- `npm run build` başarılı. Canlı: tüm route'larda başlıklar doğru,
  robots.txt/sitemap.xml 200, token'sız beacon eklenmiyor (no-op),
  yeni konsol hatası yok.

## Faz B — Ortak ActivityPage + Erişilebilirlik (tamamlandı)

### B.9 — Ortak ActivityPage
- **src/components/common/ActivityPage.jsx** (yeni): veriyle beslenen
  faaliyet sayfası; bölümler (başlık, hero, hakkında, vurgu, istatistik,
  video, profil kartı, basında biz, galeri) veriye göre opsiyonel
  render edilir. `**vurgu**` → `<strong>` mini-parser (IDEF'in bold
  ifadesi korundu).
- **src/constants/index.jsx**: `fiveGContent` → `activities` (idef +
  5g-konumlandirma) birleşik veri modeli. Yeni faaliyet = sadece veri.
- **src/pages/Idef.jsx**, **src/pages/FiveG.jsx**: ince sarmalayıcıya
  indirgendi (`<ActivityPage activity={...} />`). Route'lar (`/idef`,
  `/5g-konumlandirma`) korundu — linkler/sitemap bozulmadı.
- **Silindi**: `src/components/Events/Idef/` (7 dosya — artık
  kullanılmıyor: PageTitle, IdefHero, EventAbout, EventExplainArea,
  PresentationVideo, IdefProfileView, Gallery).
- Doğrulama: IDEF sayfası birebir aynı (8 bölüm, YouTube embed, profil
  kartı, bold "Defenders of Tomorrow (DOT)", 5 galeri görseli);
  screenshot ile teyit edildi.

### B.7 — Erişilebilirlik / geçerli HTML
- **src/components/ui/Button.jsx**: `asChild` prop'u eklendi (Radix
  deseni) — tek çocuğu klonlayıp buton sınıflarını uygular, iç içe
  interaktif eleman üretmez.
- Geçersiz `<button><a>` / `<a><button>` kullanımları düzeltildi:
  Header (Ana Sayfa), Hero (CTA'lar), Error (Ana Sayfa), Teams
  (LinkedIn), ActivityPage (profil/kaynak butonları `<span>`'e alındı).
- Doğrulama: tüm sayfalarda `button a` / `a button` iç içe sayısı **0**;
  Hero CTA'ları artık geçerli `<a>` + buton stili; görsel birebir aynı.

## Faz C — Proje Modalı, Zaman Tüneli, Haberler, Bize Katıl UX (tamamlandı)

### C.3 — Tıklanabilir proje kartları + detay modalı
- **src/components/Home/Projects.jsx**: kartlar erişilebilir `<button>`
  oldu; tıklayınca mevcut `Modal` ile detay açılıyor (şimdiye dek hiç
  gösterilmeyen `fullDescription`, `status`, `year`, `achievements`
  verisi kullanıldı). Hover ipucu + "Detayları Gör →".

### C.10 — Başarılar zaman tüneli
- **src/constants/index.jsx**: her `achievements` kaydına `year` eklendi
  (2025/2024 — ⚠️ taslak, doğrulanmalı).
- **src/components/Home/Achievements.jsx**: carousel'in altına yıl bazlı
  dikey zaman tüneli (mevcut renk/sınıf dili, carousel'e dokunulmadı).

### C.5 — Haberler / Duyurular
- **src/constants/index.jsx**: `news` dizisi (4 kayıt — doğrulanmış
  başarılardan üretildi, ⚠️ taslak metin).
- **src/components/Home/News.jsx** (yeni): `#news` bölümü, kart tasarımı
  site diliyle uyumlu; iç linkler `Link`, dış linkler yeni sekme.
- **Home.jsx**: Achievements'tan sonra eklendi. **Footer**: "Haberler"
  hızlı linki (`/#news`).

### C.11 — Bize Katıl iframe UX
- **src/pages/BizeKatil.jsx**: yüklenirken `Loading` placeholder, iframe
  `title` (a11y), `loading="lazy"`, çerçeveli kapsayıcı, postMessage
  yükseklik (pozitif kontrol), "doğrudan aç" yedek linki. Başvuru
  sistemi (form.gazisiber.org) **değiştirilmedi**.

### Doğrulama
- `npm run build` başarılı. Canlı: proje modalı (BURKAY — durum/yıl/öne
  çıkanlar), zaman tüneli (2025/2024 grupları), 4 haber kartı, temiz
  hard-reload sonrası tüm bölümler; iç içe `button/a` = 0.

## Faz D — Görsel Performansı + TR/EN i18n (tamamlandı)

### D.8 — Görsel performansı + hafif lightbox
- **src/components/common/LightboxGallery.jsx**: framer-motion kaldırıldı,
  mevcut Overlay/Backdrop + CSS animasyonu (`animate-custom-enter`) ile
  yeniden yazıldı. Esc/ok tuşları + body scroll kilidi (iyileştirme).
  **Chunk 123 kB → 1.9 kB** (gzip 40.8 kB → 0.87 kB), modüller 586 → 188.
- **framer-motion** bağımlılığı kaldırıldı (`npm uninstall`) — başka
  kullanan yoktu.
- `loading="lazy"` + `decoding="async"`: GalleryTemplate, Projects,
  Achievements (ana + thumbnail + logo), Teams, Stakeholders, Supporters,
  AboutUs, ActivityPage (about/source logoları). Hero LCP görselleri
  eager bırakıldı.

### D.6 — TR/EN i18n
- **src/i18n/translations.js** (yeni): `tr`/`en` sözlükleri (nav, hero,
  footer). ⚠️ EN metinleri **taslak** — yayın öncesi gözden geçirilmeli.
- **src/i18n/LanguageContext.jsx** (yeni): Provider + `useLanguage()`,
  `t("a.b")`, localStorage kalıcılığı, `<html lang>` güncellemesi,
  varsayılan `tr`.
- **main.jsx**: `<LanguageProvider>` ile sarıldı.
- **Header**: nav etiketleri `t()`; masaüstü + mobil + alt-sayfa
  başlığına **TR | EN** geçiş butonu.
- **Hero**, **Footer**: metinler `t()` üzerinden.
- Kapsam notu: navigasyon/hero/footer çevrildi; içerik verisi (projeler,
  başarılar, ekip, haberler, faaliyet sayfaları) aynı `t()` ile
  kademeli genişletilebilir — EN kopya onayınızı bekliyor.

### Doğrulama
- `npm run build` başarılı (188 modül). Lightbox: aç/ileri/Esc/scroll
  kilidi çalışıyor. i18n: TR↔EN canlı geçiş Header/Hero/Footer'da
  doğrulandı, localStorage kalıcı, `<html lang>` doğru. Zorlanmış
  Footer yeniden render'ında **0 konsol hatası / 0 key uyarısı**
  (önceki key uyarıları geçici HMR ara durumundan kalan eski kayıtlardı).

---

## Faz E — Görseller, Timeline, Tam i18n, /test (tamamlandı)

### E.1 — 5G galeri düzeltmesi
- Kullanıcının verdiği 2 HEIC fotoğraf (IMG_2314, IMG_2328) pure-JS
  `heic-convert` ile web JPG'ye dönüştürüldü → `public/galeri/
  5g_konum_1.jpg`, `5g_konum_2.jpg`. Araç tek seferlik kullanıldı ve
  kaldırıldı (geçici script + bağımlılık silindi).
- 5G galerisindeki tekrar eden `/galeri/5G_odul.jpg` kaldırıldı; galeri
  artık: `5G_odul.jpg` + 2 yeni foto.

### E.2 — Başarılar zaman tüneli güzelleştirme
- Her milestone'a küçük fotoğraf (achievement `mainImage`) eklendi.
- **2026** yılı eklendi — şimdilik boş, "yakında" placeholder'ı
  (içerik sonradan eklenecek).

### E.3 — Tam i18n (TR/EN tüm site)
- `LanguageContext`'e `tx({tr,en})` helper'ı eklendi (özel adlar düz
  string kalır, güvenli).
- `translations.js` kapsamlı genişletildi (nav, hero, footer, about,
  projects, achievements, news, contact, team, stakeholders,
  supporters, join, error, loading, meta).
- `constants/index.jsx` iki dilli: highlights, achievements, news,
  teamStructure rolleri, tabLabels, stats, teamContact, projectsData,
  activities (IDEF + 5G tüm içerik) — `{tr,en}`.
- Bağlanan component'ler: AboutUs, Stats, Projects, Achievements,
  News, Contact (form + toast'lar), Teams, Stakeholders, Supporters,
  ActivityPage, Header, Footer, Hero, BizeKatil, Error, Loading,
  Ekibimiz, useDocumentMeta (dil-duyarlı varsayılanlar + sayfa meta).
- Düzeltilen regresyon: Stats `key={stat.value}` artık obje olduğundan
  "[object Object]" çakışması veriyordu → `key={i}` yapıldı.
- Doğrulama: TR↔EN canlı geçiş tüm bölümlerde (home + 5G/IDEF dahil)
  çalışıyor; `hasTurkishLeftover=false`; **0 konsol hatası / 0 key
  uyarısı** (canlı yakalama ile teyit). `npm run build` başarılı.

### E.4 — Hem / hem /test/ aynı anda çalışır
- **main.jsx**: `BrowserRouter basename` **runtime'da dinamik**:
  URL `/test` ile başlıyorsa basename `/test`, değilse `/`. Böylece
  TEK deploy'dan **teamergenekon.org/** ve **teamergenekon.org/test/**
  aynı anda çalışır. (`VITE_ROUTER_BASENAME` ile açık override da
  mümkün, varsayılan kapalı.)
- Vite `base` değiştirilmedi → asset'ler kök-mutlak; her iki yol da
  kökten yüklenir, 50+ dosyada değişiklik gerekmez. `/test/` GitHub
  Pages `404.html` SPA fallback'i ile servis edilir; `<Link>`'ler
  basename sayesinde `/test` önekini korur.
- **deploy.yml**: önceki zorlayıcı `VITE_ROUTER_BASENAME` satırı geri
  alındı (deploy normal kökten yapılır).
- Doğrulama: `/` → Home + nav + başlık ✓ ; `/test/` → Home + 5 bölüm ✓ ;
  `/test/` içinde footer "Ekibimiz" tıklaması → `/test/ekibimiz`,
  Ekibimiz sayfası render ✓ (basename korunuyor).
- Not: tek Pages deploy olduğu için kök ve `/test/` aynı (en güncel)
  build'i gösterir. Kök=eski / test=yeni gibi ayrı içerik istenirse
  ayrı bir deploy stratejisi gerekir (karar sizde).

---

## Faz F — Kök = orijinal, /test/ = düzenlenmiş (ayrı içerik)

Kullanıcı netleştirdi: `teamergenekon.org/` orijinal (dokunulmamış) site,
`teamergenekon.org/test/` düzenlenmiş site olacak — **ayrı içerik**.

### F.1 — Asset yolları BASE_URL-duyarlı
- **libs/utils.js**: `asset(path)` helper'ı (harici/data URL'lere dokunmaz;
  `import.meta.env.BASE_URL`'e göre öneklendirir).
- **vite.config.js**: `base = process.env.VITE_BASE_PATH || '/'`.
- **main.jsx**: router `basename = import.meta.env.BASE_URL` (eski pathname
  tahmini kaldırıldı).
- **constants/index.jsx**: anahtar-duyarlı `pa()` transformer'ı —
  yalnızca asset alanlarını (img/image/mainImage/logo/heroImage/aboutLogo/
  bgImage + gallery dizileri) öneklendirir; rota yolları (link/to/slug),
  harici url/embed/linkedin'e **dokunmaz**.
- Hardcoded yollar `asset()` ile sarıldı: Hero (logo + `bg-[url]` →
  inline style), AboutUs, Header, Footer.

### F.2 — İki-build combined deploy
- **.github/workflows/deploy.yml**: `test` push'unda çalışır; `STABLE_REF`
  (varsayılan `main`) orijinali base `/` ile köke, bu `test` branch'ını
  base `/test/` ile `/test/` altına derler; CNAME + `.nojekyll` ekleyip
  tek Pages deploy'u yapar. (master ise `STABLE_REF` güncellenir.)

### Doğrulama
- `npm run build` (kök): asset'ler `/assets/...`, favicon `/hawk.png` —
  orijinal davranış değişmedi.
- `VITE_BASE_PATH=/test/ vite build`: `index.html`, favicon ve bundle
  `/test/...` öneki aldı; `asset()` public varlıkları `/test/...` çözer.
- Dev (base `/`): 51 görsel, **0 kırık**; hero bg + i18n çalışıyor;
  TR↔EN tam render'da **0 konsol hatası**.
- Bilinen sınır: GitHub Pages tek kök `404.html` → `/test/` alt
  rotasında doğrudan yenileme kök siteye düşer (önizleme için kabul
  edilebilir; rafgraph tekniğiyle giderilebilir — opsiyonel).

---

## Faz G — /test kaldırıldı, sadece kök site

Kullanıcı kararı: `/test/` tamamen iptal; site yalnızca **teamergenekon.org**
(kök), normal tek deploy.

- **.github/workflows/deploy.yml**: orijinal tek-build davranışına döndürüldü
  (`master`/`main` push → `npm run build` → `dist/` → tek Pages deploy).
  Cloudflare beacon secret'ı korundu.
- **vite.config.js**: `base: '/'` (env'e bağlı taban kaldırıldı).
- **main.jsx**: `BrowserRouter` basename'siz (orijinal davranış).
- **asset() / pa()**: KORUNDU. Kök tabanda (`/`) `asset("/x") === "/x"`,
  yani orijinalle birebir aynı (zararsız). 70+ referansı geri almak gereksiz
  risk olacağından bırakıldı; ileride alt-yol gerekirse altyapı hazır.
- Doğrulama: `npm run build` temiz (`/assets/...`, favicon `/hawk.png`);
  dev'de 51 görsel **0 kırık**, TR↔EN çalışıyor, **0 konsol hatası**;
  `/test` artık yok.

---

## Açık Notlar / Kullanıcı Aksiyonu Gereken
1. **Cloudflare**: Web Analytics token'ı + GitHub `CF_BEACON_TOKEN`
   secret'ı (verilene kadar analytics no-op).
2. **OG görseli**: şu an `/logo-full.png`; özel 1200×630 görsel önerilir.
   `index.html` OG/canonical URL'leri kök içindir; `/test` modunda
   güncellenebilir (SEO; "canlı görme" için kritik değil).
3. **Taslak içerikler (gözden geçirilmeli)**: achievement yılları
   (2024/2025/2026), `news` metinleri, **EN çevirileri** (özellikle
   teknik/savunma terimleri).
4. **#11**: Bize Katıl başvuru sistemi değiştirilmedi (iframe korundu).
5. **.claude/launch.json**: dev/önizleme yardımcısı; silinebilir.
6. **2026 timeline**: boş bırakıldı; içerik `constants` achievements'a
   `year: "2026"` kaydı eklenerek doldurulur.
7. **5G galeri foto boyutu**: ~1.2–1.85 MB (lazy yükleniyor); istenirse
   ileride sıkıştırılabilir.
8. **Branch**: tüm değişiklikler `test` branch'ında; commit'ler oradan
   atılacak (kullanıcı atacak).
